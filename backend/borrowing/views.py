from django.shortcuts import render

# Create your views here.
# PLACE HOLDERS

from django.http import JsonResponse

def borrow_book(request): pass
def return_book(request, pk): pass
def my_borrowed_books(request): pass
